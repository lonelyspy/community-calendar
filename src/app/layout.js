export const metadata = {
  title: "Community Calendar",
  description: "Submit and browse community events",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
